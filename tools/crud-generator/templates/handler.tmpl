package {{.Package}}

import (
	"encoding/json"
	"net/http"
	"strconv"

	"github.com/go-chi/chi/v5"
	"github.com/google/uuid"
	"github.com/your-org/pos-backend/internal/logging"
	"go.uber.org/zap"
)

// Handler handles HTTP requests for {{.GoName}}
type Handler struct {
	service *Service
	logger  *logging.Logger
}

// NewHandler creates a new {{.GoName}} handler
func NewHandler(service *Service, logger *logging.Logger) *Handler {
	return &Handler{
		service: service,
		logger:  logger,
	}
}

// Create handles POST /api/v1/organizations/{orgID}/{{.Name}}
// @Summary Create {{.Name}}
// @Description Create a new {{.Name}} record
// @Tags {{.Name}}
// @Accept json
// @Produce json
// @Security BearerAuth
// @Param orgID path string true "Organization ID"
// @Param request body Create{{.GoName}}Request true "{{.GoName}} data"
// @Success 201 {object} {{.GoName}}Response
// @Failure 400 {object} ErrorResponse
// @Failure 401 {object} ErrorResponse
// @Failure 422 {object} ErrorResponse
// @Failure 500 {object} ErrorResponse
// @Router /organizations/{orgID}/{{.Name}} [post]
func (h *Handler) Create(w http.ResponseWriter, r *http.Request) {
	ctx := r.Context()

	{{if .HasOrganization}}
	// Get organization ID from URL
	orgID, err := uuid.Parse(chi.URLParam(r, "orgID"))
	if err != nil {
		h.respondError(w, http.StatusBadRequest, "invalid organization ID", err)
		return
	}
	{{end}}

	// Parse request body
	var req Create{{.GoName}}Request
	if err := json.NewDecoder(r.Body).Decode(&req); err != nil {
		h.respondError(w, http.StatusBadRequest, "invalid request body", err)
		return
	}

	// Call service
	{{if .HasOrganization}}
	result, err := h.service.Create(ctx, orgID, &req)
	{{else}}
	result, err := h.service.Create(ctx, &req)
	{{end}}
	if err != nil {
		h.logger.Error("failed to create {{.Name}}", zap.Error(err))
		h.respondError(w, http.StatusUnprocessableEntity, "failed to create {{.Name}}", err)
		return
	}

	h.respondJSON(w, http.StatusCreated, result)
}

// GetByID handles GET /api/v1/organizations/{orgID}/{{.Name}}/{id}
// @Summary Get {{.Name}} by ID
// @Description Retrieve a {{.Name}} by its ID
// @Tags {{.Name}}
// @Produce json
// @Security BearerAuth
// @Param orgID path string true "Organization ID"
// @Param id path string true "{{.GoName}} ID"
// @Success 200 {object} {{.GoName}}Response
// @Failure 400 {object} ErrorResponse
// @Failure 401 {object} ErrorResponse
// @Failure 404 {object} ErrorResponse
// @Failure 500 {object} ErrorResponse
// @Router /organizations/{orgID}/{{.Name}}/{id} [get]
func (h *Handler) GetByID(w http.ResponseWriter, r *http.Request) {
	ctx := r.Context()

	{{if .HasOrganization}}
	// Get organization ID from URL
	orgID, err := uuid.Parse(chi.URLParam(r, "orgID"))
	if err != nil {
		h.respondError(w, http.StatusBadRequest, "invalid organization ID", err)
		return
	}
	{{end}}

	// Get ID from URL
	id, err := uuid.Parse(chi.URLParam(r, "id"))
	if err != nil {
		h.respondError(w, http.StatusBadRequest, "invalid {{.Name}} ID", err)
		return
	}

	// Call service
	{{if .HasOrganization}}
	result, err := h.service.GetByID(ctx, orgID, id)
	{{else}}
	result, err := h.service.GetByID(ctx, id)
	{{end}}
	if err != nil {
		h.logger.Error("failed to get {{.Name}}", zap.Error(err), zap.String("id", id.String()))
		h.respondError(w, http.StatusNotFound, "{{.Name}} not found", err)
		return
	}

	h.respondJSON(w, http.StatusOK, result)
}

// List handles GET /api/v1/organizations/{orgID}/{{.Name}}
// @Summary List {{.Name}}
// @Description Retrieve a paginated list of {{.Name}} records
// @Tags {{.Name}}
// @Produce json
// @Security BearerAuth
// @Param orgID path string true "Organization ID"
// @Param page query int false "Page number" default(1)
// @Param limit query int false "Items per page" default(20)
// @Success 200 {object} {{.GoName}}ListResponse
// @Failure 400 {object} ErrorResponse
// @Failure 401 {object} ErrorResponse
// @Failure 500 {object} ErrorResponse
// @Router /organizations/{orgID}/{{.Name}} [get]
func (h *Handler) List(w http.ResponseWriter, r *http.Request) {
	ctx := r.Context()

	{{if .HasOrganization}}
	// Get organization ID from URL
	orgID, err := uuid.Parse(chi.URLParam(r, "orgID"))
	if err != nil {
		h.respondError(w, http.StatusBadRequest, "invalid organization ID", err)
		return
	}
	{{end}}

	// Get pagination parameters
	page, _ := strconv.Atoi(r.URL.Query().Get("page"))
	if page < 1 {
		page = 1
	}

	limit, _ := strconv.Atoi(r.URL.Query().Get("limit"))
	if limit < 1 || limit > 100 {
		limit = 20
	}

	// Call service
	{{if .HasOrganization}}
	result, err := h.service.List(ctx, orgID, page, limit)
	{{else}}
	result, err := h.service.List(ctx, page, limit)
	{{end}}
	if err != nil {
		h.logger.Error("failed to list {{.Name}}", zap.Error(err))
		h.respondError(w, http.StatusInternalServerError, "failed to list {{.Name}}", err)
		return
	}

	h.respondJSON(w, http.StatusOK, result)
}

// Update handles PUT /api/v1/organizations/{orgID}/{{.Name}}/{id}
// @Summary Update {{.Name}}
// @Description Update an existing {{.Name}} record
// @Tags {{.Name}}
// @Accept json
// @Produce json
// @Security BearerAuth
// @Param orgID path string true "Organization ID"
// @Param id path string true "{{.GoName}} ID"
// @Param request body Update{{.GoName}}Request true "{{.GoName}} data"
// @Success 200 {object} {{.GoName}}Response
// @Failure 400 {object} ErrorResponse
// @Failure 401 {object} ErrorResponse
// @Failure 404 {object} ErrorResponse
// @Failure 422 {object} ErrorResponse
// @Failure 500 {object} ErrorResponse
// @Router /organizations/{orgID}/{{.Name}}/{id} [put]
func (h *Handler) Update(w http.ResponseWriter, r *http.Request) {
	ctx := r.Context()

	{{if .HasOrganization}}
	// Get organization ID from URL
	orgID, err := uuid.Parse(chi.URLParam(r, "orgID"))
	if err != nil {
		h.respondError(w, http.StatusBadRequest, "invalid organization ID", err)
		return
	}
	{{end}}

	// Get ID from URL
	id, err := uuid.Parse(chi.URLParam(r, "id"))
	if err != nil {
		h.respondError(w, http.StatusBadRequest, "invalid {{.Name}} ID", err)
		return
	}

	// Parse request body
	var req Update{{.GoName}}Request
	if err := json.NewDecoder(r.Body).Decode(&req); err != nil {
		h.respondError(w, http.StatusBadRequest, "invalid request body", err)
		return
	}

	// Call service
	{{if .HasOrganization}}
	result, err := h.service.Update(ctx, orgID, id, &req)
	{{else}}
	result, err := h.service.Update(ctx, id, &req)
	{{end}}
	if err != nil {
		h.logger.Error("failed to update {{.Name}}", zap.Error(err), zap.String("id", id.String()))
		h.respondError(w, http.StatusUnprocessableEntity, "failed to update {{.Name}}", err)
		return
	}

	h.respondJSON(w, http.StatusOK, result)
}

// Delete handles DELETE /api/v1/organizations/{orgID}/{{.Name}}/{id}
// @Summary Delete {{.Name}}
// @Description Delete a {{.Name}} record
// @Tags {{.Name}}
// @Produce json
// @Security BearerAuth
// @Param orgID path string true "Organization ID"
// @Param id path string true "{{.GoName}} ID"
// @Success 204
// @Failure 400 {object} ErrorResponse
// @Failure 401 {object} ErrorResponse
// @Failure 404 {object} ErrorResponse
// @Failure 500 {object} ErrorResponse
// @Router /organizations/{orgID}/{{.Name}}/{id} [delete]
func (h *Handler) Delete(w http.ResponseWriter, r *http.Request) {
	ctx := r.Context()

	{{if .HasOrganization}}
	// Get organization ID from URL
	orgID, err := uuid.Parse(chi.URLParam(r, "orgID"))
	if err != nil {
		h.respondError(w, http.StatusBadRequest, "invalid organization ID", err)
		return
	}
	{{end}}

	// Get ID from URL
	id, err := uuid.Parse(chi.URLParam(r, "id"))
	if err != nil {
		h.respondError(w, http.StatusBadRequest, "invalid {{.Name}} ID", err)
		return
	}

	// Call service
	{{if .HasOrganization}}
	err = h.service.Delete(ctx, orgID, id)
	{{else}}
	err = h.service.Delete(ctx, id)
	{{end}}
	if err != nil {
		h.logger.Error("failed to delete {{.Name}}", zap.Error(err), zap.String("id", id.String()))
		h.respondError(w, http.StatusUnprocessableEntity, "failed to delete {{.Name}}", err)
		return
	}

	w.WriteHeader(http.StatusNoContent)
}

// respondJSON writes a JSON response
func (h *Handler) respondJSON(w http.ResponseWriter, status int, data interface{}) {
	w.Header().Set("Content-Type", "application/json")
	w.WriteHeader(status)

	if data != nil {
		if err := json.NewEncoder(w).Encode(data); err != nil {
			h.logger.Error("failed to encode response", zap.Error(err))
		}
	}
}

// respondError writes an error response
func (h *Handler) respondError(w http.ResponseWriter, status int, message string, err error) {
	w.Header().Set("Content-Type", "application/json")
	w.WriteHeader(status)

	response := ErrorResponse{
		Error:   message,
		Message: err.Error(),
	}

	if err := json.NewEncoder(w).Encode(response); err != nil {
		h.logger.Error("failed to encode error response", zap.Error(err))
	}
}

// ErrorResponse represents an error response
type ErrorResponse struct {
	Error   string `json:"error"`
	Message string `json:"message,omitempty"`
}
